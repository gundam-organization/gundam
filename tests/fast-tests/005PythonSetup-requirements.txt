# Python packages needed by the GUNDAM python tests.
#
# These are installed into the per-run virtual environment created by
# 005PythonSetup.sh, which reads this file from its own directory.  Add a
# line here when a test needs a new package, and say which test needs it.
# Each requirement is smoke tested by importing it, so the name must also be
# the module name.
#
# Not listed here because they are not pip packages: the tests also need the
# GUNDAM python interface on $PYTHONPATH (provided by the install setup.sh),
# and fast-tests/212EvalFromLibCompatibility.py needs a g++ compiler.

uproot    # writes the ROOT input trees read by every fast-tests/21*.py test
numpy     # used by the helper script generated by 213ExternalWeightResponse.py
